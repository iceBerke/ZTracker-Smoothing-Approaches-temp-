# prompt_outputs INDEX

One appended line per entry. Infrastructure: this file carries no number of its
own and is the only file here that is appended to rather than added.

Entries are append-only. A re-run of a task gets a new number and states at the
top of its own file which number it supersedes and why. See `RULES.md` section 2.

Format: `NNN — slug — phases present — date — what the task did or found.`

- 001 — zsmooth-recon — audit — 2026-09-09 — Read-only reconnaissance of the tool, its environment and its dataset; confirmed determinism, gap-awareness, outlier removal and the derivation stamp's blind spot, and contradicted four handover claims including "every parameter is derived from the data" and "WINDOW_NOT_DETERMINED fires for every method".
- 002 — zsmooth-governance — phaseB — 2026-09-10 — Created RULES.md and CLAUDE.md, corrected the .gitignore input-identity comment, untracked out/, and annotated the one README block known stale; found that Stage 1c is not the only place input files interact and that per-track estimation fails on roughly half of all tracks, not merely via negative Gamma.
